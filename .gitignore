# ── OS ───────────────────────────────────────────
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/
*.lnk

# ── Editor / IDE ────────────────────────────────
.vscode/
.idea/
.history/
*.swp
*.swo
*~

# ── Env & secrets ───────────────────────────────
.env
.env.*
!.env.example

# ── Logs ────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# ── Node ────────────────────────────────────────
node_modules/

# ── Build / dist / cache ────────────────────────
dist/
build/
out/
.next/
.nuxt/
.cache/
.turbo/
.parcel-cache/
coverage/
*.lcov

# ── Playwright (本地 E2E 验证,见 swimlane-dev-history) ──
test-results/
playwright-report/
blob-report/
playwright/.cache/

# ── Temp ────────────────────────────────────────
*.tmp
*.temp
.tmp/
tmp/

# ── gh-pages orphan 分支 worktree(见 NewSD dev-history 部署与维护机制) ──
*-ghpages-tmp/

# ── BMad/agent 工具:per-IDE 技能同步副本为可再生本地安装 ──
#    canonical 源 = _bmad/(tracked) + .github/agents/(tracked),用 BMad installer 或 sync 技能重装
#    想跟踪则删以下三行 + git add -f
.claude/skills/
.cline/skills/
.agents/skills/
