# 04.1-profile-save (Profile/存档)

## Page Metadata

| Property | Value |
|----------|-------|
| **Scenario** | 04: Jaron 管理存档与进度 |
| **Page Number** | 04.1 |
| **Platform** | Desktop |

## Overview

**Page Purpose:** 看存档状态 + 管理存档。显示 profile class/alignment、双轨 per-track XP、解锁态、牌组历史 passes、画布态草稿(per-level)、校验日志 failures/降级（诊断详情 owner 可见 D1；GS1 passes/failures 分离：passes->牌组历史 / failures->校验日志）。管理：export JSON 备份/迁移 / import（Zod 校验+上限钳制+画布态 graph 校验）/ reset（护栏 G3 export-first + G2 type-confirm + G4 danger-zone）。

**Entry Context:** 从全局导航 / 总览(#1) 点 profile/存档入口进入。

**Exit Action:** 完成管理 -> 回 #1 总览 / 全局导航。

**On-Page Interactions:**
- 浏览存档状态（profile/双轨 XP/解锁态/守序 alignment）
- 查看/恢复 per-level 画布态草稿（resume，auto-save CS1）
- 查看牌组历史 passes（成功态，FS1=A user）
- 查看校验日志 failures + 降级（诊断详情，owner 可见 [D1 决策 a]，与牌组历史 passes 分离）
- export JSON（备份/迁移）
- import JSON（Zod schema 校验 + 上限钳制 + 画布态 graph 校验，信任边界）
- reset（G3 export-first 强制先导出 -> G2 type-confirm -> G4 danger-zone 确认）
- 切换/查看 passes/failures 数据角色视角（GS1 role-separation，非战士/圣骑轨）

## First Step Context

**User Situation (Q3):** Jaron（PRIMARY 双轨学徒, 三位一体）, 晚间桌面自学; 玩了一阵想备份/迁移/重置; MVP 纯本地无云 sync, 进度只在本机, 要自己管。

**Mental State (Q4):**
- **Hope:** 掌控自己进度数据--备份/迁移/重来都不丢, 存档状态一目了然（双轨分离清晰）。
- **Worry:** 误操作丢进度（reset 误触）/ 导入坏 JSON 污染 / passes/failures 数据角色混淆。

**Device (Q5):** Desktop

**Discovery Method (Q6):** 从全局导航/总览(#1) 点 profile/存档入口进 #8。单用户本地工具。

## Spec Carry-overs (landing here)

- **CS3:** #8 拥 profile/XP/画布态 schema（数据模型定义）；#5/#6 经 invariant 消费。
- **CS6:** #6 XP 权威；#8 显示派生 XP（不写）。
- **GS1:** passes/failures 数据角色 IA - role-separation UI 明示（failures->校验日志 / passes->牌组历史）。
- **GS3:** reset/import per-store scope（牌组历史 + 校验日志 + 画布态）。
- **FS1=A:** 牌组历史=user passes / 校验日志=诊断详情 failures+降级（owner 可见 [D1 决策 a]，非 dev-only）/ 画布态=工作草稿（user 可变, auto-save CS1）；分离显示三 store。
- **ADR-6:** localStorage(profile/XP/解锁态) + IndexedDB(牌组历史/校验日志/画布态草稿) + schema_version；storage 抽象层。
- **导入信任边界:** Zod + 上限钳制（XP/解锁不超当前阶段上限）+ 画布态 graph 校验（nodes≤32 / cards ⊆ unlocked / valid edges）。
- **reset 护栏:** G3 export-first + G2 type-confirm + G4 danger-zone。
- **守序 alignment:** profile state(class: warrior/paladin, both->lawful) + 显示。
- **onboarding=mode:** profile 创建于首玩关（auto-create，条件=profile 不存在时[R1]，idempotent/atomic check-then-create 防 double[R7]；详 02）。
- **解锁纯函数:** XP->牌（reset 改 XP 派生解锁）。
- **画布态（复查-C1=A 决策）:** per-level 草稿 = 第 3 IndexedDB store（auto-save CS1 C2↔C4, resume）；#8 view/export/import/reset 纳入；3 store 3 角色不混。

---
_Generated with Whiteport Design Studio (WDS v6) - Freya (WDS Designer)_
_step-05 first step auto-processed (2026-08-03)_
_Document language: Chinese_