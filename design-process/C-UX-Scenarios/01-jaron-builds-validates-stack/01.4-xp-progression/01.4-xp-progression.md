# 01.4-xp-progression (XP/进阶)

## Page Metadata

| Property | Value |
|----------|-------|
| **Scenario** | 01: Jaron 组装并校验云架构栈 |
| **Page Number** | 01.4 |
| **Platform** | Desktop |

## Overview

**Page Purpose:** Jaron 在 01.3 画布过关后进入 #6 XP/进阶。这步 = 进阶结算：记录本关 XP（=WA 分，B3 产出，CS6 #6 单一真源写入）+ 派生解锁（pass N min WA -> unlock N+1 cards，纯函数 `per-track XP -> 可用牌集`，阈值表 JSON bundle）+ 显示进阶（累计 per-track XP / 关卡通关状态 / 新解锁牌 / 下一关准入预览）。若触发：通关总结 = Tier-3 overlay on #6 + #6 徽记（非页表面）。

**Entry Context:** 从 01.3 画布过关（提交校验过 A3+B1+B3）后进入。仅过关才进 #6；降级态（A3-only，本局无 XP）= #4 halt + visible report，不进 #6（Q3=3a halt accepted）。

**Exit Action:** 结算完 + 看完进阶/解锁 -> 点「回课程地图 / 选下一关」CTA -> 回 #3 课程地图(01.1) 选下一未通关关。= 闭合 per-level 循环（01.1->01.2->01.3->01.4->01.1），sunshine path 终结 ✓。

**On-Page Interactions:**
- XP 结算显示（本关 WA 分 / 累计 per-track XP）
- 解锁提示（新解锁牌集）
- 进阶视图（关卡通关状态 / 下一关准入预览）
- 通关总结 = Tier-3 overlay on #6 + 徽记（per-level 触发；W2/P2 轨末加轨通关标记）
- 降级态不达此页（#4 halt + visible report）

## Spec Carry-overs (landing here)

- **CS6:** #6 XP 权威单一真源（写 XP）；#1/#8 派生。
- **CS3:** #6 消费 #8 schema（profile/XP），写 XP + 解锁态。
- **阈值不变量:** pass N(min WA) -> unlock N+1；纯函数(per-track XP->牌)，阈值表 JSON bundle（可单测）。
- **通关总结 = Tier-3 overlay on #6 + #6 徽记**（非页表面）。
- **轴C 里程碑:** 解锁 = 里程碑推进。
- **降级态:** #4 halt + visible report，不进 #6（本局无 XP/不解锁）。

## Phase-4 Flagged Decisions (working assumption, 不锁)

- **通关总结 触发 scope:** per-level（全 4 关 W1/W2/P1/P2 各一）+ W2/P2 轨末 overlay 内加「轨通关」标记（不另立徽记）；per-track 专属徽记延 v1.x+；Tier-3 层级语义待定义。Phase 4 渲染探针验频率手感后定。
- **XP 多参考聚合:** 固定（per-level pass 给固定 WA 分，前提 = 多参考等权/同 level 多参考架构 WA 权重相同）；取最高延 v1.x+（若引入 better-than-valid 优化教学）；与参考架构规格 schema（references WA 加权）Phase 4 同定。project-context 标 ref schema 为 MVP critical path。
- **全通关 (O1) 终端态 [10人模拟 B1+B2]:** 4 关全过 = O1 双轨通关 = MVP 目标达成。01.1 全通关态 display（无未通关关可选，显全通关标记）；01.4 Tier-3 escalated overlay on #6（per-level/per-track 之外第 3 层 terminal，全通关庆祝）。Phase 4 渲染定。

---
_Generated with Whiteport Design Studio (WDS v6) - Freya (WDS Designer)_
_step-05 Per-Step Dialog (Suggest mode, 2026-08-03)_
_Document language: Chinese_