# 01.1-course-map (课程地图)

## Page Metadata

| Property | Value |
|----------|-------|
| **Scenario** | 01: Jaron 组装并校验云架构栈 |
| **Page Number** | 01.1 |
| **Platform** | Desktop |

## Overview

**Page Purpose:** 看双轨4关地图(战士 W1/W2, 圣骑 P1/P2)，进入关卡。两 entry state: first-time(从 02.2 handoff, track+entry level 预选[CS1 C1<->C2 R8/T1], 落预选关 W1/P1) / returning(选一未通关关卡进入)；地图显示通关状态 + 准入(derived from unlock invariant: required_cards ⊆ unlocked_cards，纯函数 XP->牌)。

**Entry Context:** 两 entry state: first-time = 从 02.2 选角选轨确认(传 track+entry level[W1/P1] per CS1 C1<->C2 R8[T1]) -> 落预选 entry level; returning = 开应用落 #1 总览(hub, 02 R6/E10') -> 导航 #3 选未通关关。[C1 修正: 01.1 非应用首落页; 02 确立开应用落 #1 hub, 01.1 经 #1 导航或 02.2 handoff 到达]

**Exit Action:** 选定一未通关关卡 -> 进入 #5 牌池(01.2)

**On-Page Interactions:**
- 浏览双轨4关地图(战士轨 / 圣骑士轨两条轨)
- 查看每关通关状态(未通/已通) + 准入标识(source-agnostic guard)
- 选定一关进入(TBD detail: 点击/选中确认交互)

## First Step Context

**User Situation (Q3):** Jaron(PRIMARY 双轨学徒, 三位一体=产品所有者+设计伙伴+目标用户), 晚间自家桌面自学时段; 已过 onboarding(02 选角选轨) 选好轨, 想主动练"此架构是否合理"判断, 非被动读文档/牌面。

**Mental State (Q4):**
- **Hope:** 拖牌组装一架构, 双层校验给确定反馈(过/不过+具体错在哪), 确认"我的架构判断对了"。
- **Worry:** 架构看似合理实则 soundness 缺失(选错组件/漏关键依赖/控制面归属误判), 校验不过却看不懂为何, 等于又"学错"。

**Device (Q5):** Desktop

**Discovery Method (Q6):** 桌面打开本地 CloudTech(纯前端离线, 无后端)。单用户工具, 到达=开应用落 #1 总览(hub) -> 导航 #3(returning); first-time = 从 02.2 handoff。不经搜索引擎/外链。

## Spec Carry-overs (landing here)

- **[T1 传导]** 01.1 须处理两 entry state: first-time(从 02.2 选角选轨确认, 传 track+entry level[W1/P1] per CS1 C1<->C2 R8, 落预选 entry level) / returning(开应用落 #1 -> 导航 #3 选未通关关)。
- **CS1 C1<->C2[R8]:** 02.2 exit -> 01.1 entry(first-time: track+level 预选 -> 01.1 落对应关 + auto-create profile[02 R1/R7])。
- **准入 guard(锁定决策6):** #4-entry guard(source-agnostic, required_cards ⊆ unlocked_cards), #3 仅显示准入(derived), 不执行。
- **A1[R11]:** 首玩关 entry 依赖 A1 含首关参考架构所需牌(W1 S3+CloudFront / P1 EC2+VPC+EBS)。

---

_Generated with Whiteport Design Studio (WDS v6) - Freya (WDS Designer)_
_step-05 first step auto-processed (2026-08-03)_
_Document language: Chinese_